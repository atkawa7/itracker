# Override default properties in application-context.xml

dataSource.jdbcUrl=jdbc:hsqldb:mem:itrackerdev-test
dataSource.user=sa
dataSource.password=
dataSource.driverClass=org.hsqldb.jdbcDriver
dataSource.password=
# many connections are needed at the same time
dataSource.maxPoolSize=2000


configurationProperties.location=classpath:configuration.properties